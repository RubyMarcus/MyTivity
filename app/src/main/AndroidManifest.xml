<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.apia22018.sportactivities">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_on_bakgrund"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_on_bakgrund"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".screens.listActivities.ActivitiesActivity" />
        <activity
            android:name=".screens.splash.SplashActivity"
            android:theme="@style/SplashTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.newActivity.AddActivityActivity" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_place_api" />
        <activity android:name=".screens.detailActivityContainer.DetailActivityContainerActivity" />
        <activity android:name=".screens.splash.LoginActivity" />
        <activity android:name=".screens.login.LoginActivity" />

        <activity android:name=".screens.mapActivities.mapActivity"></activity>
    </application>

</manifest>
